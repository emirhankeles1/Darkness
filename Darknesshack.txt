local player = game.Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")

local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

-- Karakter değişince yeni humanoid'i al
local function onCharacterAdded(char)
    character = char
    humanoid = character:WaitForChild("Humanoid")
end

player.CharacterAdded:Connect(onCharacterAdded)

local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "SidebarGui"
screenGui.ResetOnSpawn = false

local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 350, 0, 300)
mainFrame.Position = UDim2.new(0, 50, 0, 50)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
mainFrame.Active = true
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 20)

local toggleBtn = Instance.new("TextButton", mainFrame)
toggleBtn.Size = UDim2.new(0, 30, 0, 30)
toggleBtn.Position = UDim2.new(1, -35, 0, 5)
toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
toggleBtn.Text = "X"
toggleBtn.TextColor3 = Color3.new(1, 1, 1)
toggleBtn.Font = Enum.Font.GothamBold
toggleBtn.TextSize = 20
toggleBtn.AutoButtonColor = false
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 6)

toggleBtn.MouseEnter:Connect(function()
    toggleBtn.BackgroundColor3 = Color3.fromRGB(230, 90, 90)
end)
toggleBtn.MouseLeave:Connect(function()
    toggleBtn.BackgroundColor3 = Color3.fromRGB(200, 60, 60)
end)

local openBtn = Instance.new("TextButton", screenGui)
openBtn.Size = UDim2.new(0, 30, 0, 30)
openBtn.Position = UDim2.new(0, 50, 0, 50)
openBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 130)
openBtn.Text = "▶"
openBtn.TextColor3 = Color3.new(1, 1, 1)
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 20
openBtn.Visible = false
Instance.new("UICorner", openBtn).CornerRadius = UDim.new(0, 6)

local guiVisible = true
toggleBtn.MouseButton1Click:Connect(function()
    guiVisible = not guiVisible
    mainFrame.Visible = guiVisible
    openBtn.Visible = not guiVisible
end)

openBtn.MouseButton1Click:Connect(function()
    guiVisible = true
    mainFrame.Visible = true
    openBtn.Visible = false
end)

-- Menü paneli artık ScrollFrame
local menuPanel = Instance.new("ScrollingFrame", mainFrame)
menuPanel.Size = UDim2.new(0, 100, 1, 0)
menuPanel.Position = UDim2.new(0, 0, 0, 0)
menuPanel.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
menuPanel.CanvasSize = UDim2.new(0, 0, 1, 100) -- İçerik fazla ise scroll çıkacak
menuPanel.ScrollBarThickness = 8
Instance.new("UICorner", menuPanel).CornerRadius = UDim.new(0, 20)

local function createMenuButton(text, posY)
    local btn = Instance.new("TextButton", menuPanel)
    btn.Size = UDim2.new(1, -20, 0, 50)
    btn.Position = UDim2.new(0, 10, 0, posY)
    btn.BackgroundColor3 = Color3.fromRGB(70, 70, 90)
    btn.Text = text
    btn.TextColor3 = Color3.new(1, 1, 1)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 22
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)

    btn.MouseEnter:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(90, 90, 110)
    end)
    btn.MouseLeave:Connect(function()
        btn.BackgroundColor3 = Color3.fromRGB(70, 70, 90)
    end)

    return btn
end

-- Menü butonları (yukarıdan aşağıya artan pozisyonları)
local homeBtn = createMenuButton("Home", 20)
local jumpBtn = createMenuButton("Jump", 80)
local speedBtn = createMenuButton("Speed", 140)
local noclipBtn = createMenuButton("Noclip", 200)
local espBtn = createMenuButton("ESP", 260)

local contentPanel = Instance.new("Frame", mainFrame)
contentPanel.Size = UDim2.new(1, -100, 1, 0)
contentPanel.Position = UDim2.new(0, 100, 0, 0)
contentPanel.BackgroundTransparency = 1
Instance.new("UICorner", contentPanel).CornerRadius = UDim.new(0, 20)

local contentTitle = Instance.new("TextLabel", contentPanel)
contentTitle.Size = UDim2.new(1, 0, 0, 40)
contentTitle.Position = UDim2.new(0, 0, 0, 0)
contentTitle.BackgroundTransparency = 1
contentTitle.TextColor3 = Color3.new(1,1,1)
contentTitle.Font = Enum.Font.GothamBold
contentTitle.TextSize = 24
contentTitle.Text = "Home"

local contentArea = Instance.new("Frame", contentPanel)
contentArea.Size = UDim2.new(1, -20, 1, -50)
contentArea.Position = UDim2.new(0, 10, 0, 50)
contentArea.BackgroundTransparency = 1

-- Home İçerik: Buton eklendi
-- Home İçerik: ScrollFrame olarak değiştirildi
local homeContent = Instance.new("ScrollingFrame", contentArea)
homeContent.Size = UDim2.new(1, 0, 1, 0)
homeContent.BackgroundTransparency = 1
homeContent.Visible = true
homeContent.CanvasSize = UDim2.new(0, 0, 0, 300) -- Gerekirse artır
homeContent.ScrollBarThickness = 8
Instance.new("UICorner", homeContent).CornerRadius = UDim.new(0, 12)

local welcomeLabel = Instance.new("TextLabel", homeContent)
welcomeLabel.Size = UDim2.new(1, 0, 0, 40)
welcomeLabel.BackgroundTransparency = 1
welcomeLabel.TextColor3 = Color3.new(0.8, 0.8, 0.8)
welcomeLabel.Text = "darkeness hack"
welcomeLabel.Font = Enum.Font.Gotham
welcomeLabel.TextSize = 18

local jumpBtnHome = Instance.new("TextButton", homeContent)
jumpBtnHome.Size = UDim2.new(0, 200, 0, 50)
jumpBtnHome.Position = UDim2.new(0, 10, 0, 60)
jumpBtnHome.BackgroundColor3 = Color3.fromRGB(60, 200, 130)
jumpBtnHome.TextColor3 = Color3.new(1, 1, 1)
jumpBtnHome.Font = Enum.Font.GothamBold
jumpBtnHome.TextSize = 22
jumpBtnHome.Text = "f3x"
Instance.new("UICorner", jumpBtnHome).CornerRadius = UDim.new(0, 12)

-- Butona tıklanınca script çalıştırma
jumpBtnHome.MouseButton1Click:Connect(function()
	local scriptContent = game:GetObjects("rbxassetid://6695644299")[1]
	loadstring(scriptContent.Source)()
end)

local speedBtnHome = Instance.new("TextButton", homeContent)
speedBtnHome.Size = UDim2.new(0, 200, 0, 50)
speedBtnHome.Position = UDim2.new(0, 10, 0, 120)
speedBtnHome.BackgroundColor3 = Color3.fromRGB(60, 130, 200)
speedBtnHome.TextColor3 = Color3.new(1, 1, 1)
speedBtnHome.Font = Enum.Font.GothamBold
speedBtnHome.TextSize = 22
speedBtnHome.Text = "owlhub"
Instance.new("UICorner", speedBtnHome).CornerRadius = UDim.new(0, 12)

-- Butona tıklanınca scripti çalıştır
speedBtnHome.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/CriShoux/OwlHub/master/OwlHub.txt"))()
end)

local denemeBtnHome = Instance.new("TextButton", homeContent)
denemeBtnHome.Size = UDim2.new(0, 200, 0, 50)
denemeBtnHome.Position = UDim2.new(0, 10, 0, 180)
denemeBtnHome.BackgroundColor3 = Color3.fromRGB(60, 130, 200)
denemeBtnHome.TextColor3 = Color3.new(1, 1, 1)
denemeBtnHome.Font = Enum.Font.GothamBold
denemeBtnHome.TextSize = 22
denemeBtnHome.Text = "dex"
Instance.new("UICorner", denemeBtnHome).CornerRadius = UDim.new(0, 12)

-- Butona tıklanınca scripti çalıştır
denemeBtnHome.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-REAL-DEX-EXPLORER-DONT-BE-SCAMMED-41800"))()
end)

local selamBtnHome = Instance.new("TextButton", homeContent)
selamBtnHome.Size = UDim2.new(0, 200, 0, 50)
selamBtnHome.Position = UDim2.new(0, 10, 0, 240)
selamBtnHome.BackgroundColor3 = Color3.fromRGB(60, 130, 200)
selamBtnHome.TextColor3 = Color3.new(1, 1, 1)
selamBtnHome.Font = Enum.Font.GothamBold
selamBtnHome.TextSize = 22
selamBtnHome.Text = "admin"
Instance.new("UICorner", denemeBtnHome).CornerRadius = UDim.new(0, 12)

-- Butona tıklanınca scripti çalıştır
selamBtnHome.MouseButton1Click:Connect(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
end)

jumpBtnHome.MouseButton1Click:Connect(function()
    setActiveMenu("Jump")
end)

speedBtnHome.MouseButton1Click:Connect(function()
    setActiveMenu("Speed")
end)

denemeBtnHome.MouseButton1Click:Connect(function()
    setActiveMenu("deneme")
end)

selamBtnHome.MouseButton1Click:Connect(function()
    setActiveMenu("selam")
end)

-- Jump İçerik
local jumpContent = Instance.new("Frame", contentArea)
jumpContent.Size = UDim2.new(1, 0, 1, 0)
jumpContent.BackgroundTransparency = 1
jumpContent.Visible = false

local jumpLabel = Instance.new("TextLabel", jumpContent)
jumpLabel.Size = UDim2.new(1, 0, 0, 40)
jumpLabel.BackgroundTransparency = 1
jumpLabel.Text = "Jump modu ayarı"
jumpLabel.Font = Enum.Font.GothamBold
jumpLabel.TextSize = 22
jumpLabel.TextColor3 = Color3.new(1, 1, 1)

local jumpToggleBtn = Instance.new("TextButton", jumpContent)
jumpToggleBtn.Size = UDim2.new(0, 200, 0, 50)
jumpToggleBtn.Position = UDim2.new(0.5, -100, 0, 50)
jumpToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 130)
jumpToggleBtn.TextColor3 = Color3.new(1,1,1)
jumpToggleBtn.Font = Enum.Font.GothamBold
jumpToggleBtn.TextSize = 22
jumpToggleBtn.Text = "Jump: 50 (Düşük)"
Instance.new("UICorner", jumpToggleBtn).CornerRadius = UDim.new(0, 12)

jumpToggleBtn.MouseEnter:Connect(function()
    jumpToggleBtn.BackgroundColor3 = Color3.fromRGB(90, 230, 170)
end)
jumpToggleBtn.MouseLeave:Connect(function()
    jumpToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 200, 130)
end)

jumpToggleBtn.MouseButton1Click:Connect(function()
    if humanoid.JumpPower == 50 then
        humanoid.JumpPower = 100
        jumpToggleBtn.Text = "Jump: 100 (Yüksek)"
    else
        humanoid.JumpPower = 50
        jumpToggleBtn.Text = "Jump: 50 (Düşük)"
    end
end)

-- Speed İçerik
local speedContent = Instance.new("Frame", contentArea)
speedContent.Size = UDim2.new(1, 0, 1, 0)
speedContent.BackgroundTransparency = 1
speedContent.Visible = false

local speedToggleBtn = Instance.new("TextButton", speedContent)
speedToggleBtn.Size = UDim2.new(0, 200, 0, 50)
speedToggleBtn.Position = UDim2.new(0.5, -100, 0, 50)
speedToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 200)
speedToggleBtn.TextColor3 = Color3.new(1,1,1)
speedToggleBtn.Font = Enum.Font.GothamBold
speedToggleBtn.TextSize = 22
speedToggleBtn.Text = "Speed: 16 (Varsayılan)"
Instance.new("UICorner", speedToggleBtn).CornerRadius = UDim.new(0, 12)

speedToggleBtn.MouseEnter:Connect(function()
    speedToggleBtn.BackgroundColor3 = Color3.fromRGB(90, 170, 230)
end)
speedToggleBtn.MouseLeave:Connect(function()
    speedToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 130, 200)
end)

speedToggleBtn.MouseButton1Click:Connect(function()
    if humanoid.WalkSpeed == 16 then
        humanoid.WalkSpeed = 50
        speedToggleBtn.Text = "Speed: 50 (Hızlı)"
    else
        humanoid.WalkSpeed = 16
        speedToggleBtn.Text = "Speed: 16 (Varsayılan)"
    end
end)

-- Noclip İçerik
local noclipContent = Instance.new("Frame", contentArea)
noclipContent.Size = UDim2.new(1, 0, 1, 0)
noclipContent.BackgroundTransparency = 1
noclipContent.Visible = false

local noclipToggleBtn = Instance.new("TextButton", noclipContent)
noclipToggleBtn.Size = UDim2.new(0, 200, 0, 50)
noclipToggleBtn.Position = UDim2.new(0.5, -100, 0, 50)
noclipToggleBtn.BackgroundColor3 = Color3.fromRGB(200, 130, 60)
noclipToggleBtn.TextColor3 = Color3.new(1,1,1)
noclipToggleBtn.Font = Enum.Font.GothamBold
noclipToggleBtn.TextSize = 22
noclipToggleBtn.Text = "Noclip: Kapalı"
Instance.new("UICorner", noclipToggleBtn).CornerRadius = UDim.new(0, 12)

local noclipEnabled = false

noclipToggleBtn.MouseButton1Click:Connect(function()
    noclipEnabled = not noclipEnabled
    noclipToggleBtn.Text = noclipEnabled and "Noclip: Açık" or "Noclip: Kapalı"
end)

RunService.Stepped:Connect(function()
    if noclipEnabled and character then
        for _, part in pairs(character:GetDescendants()) do
            if part:IsA("BasePart") then
                part.CanCollide = false
            end
        end
    end
end)

-- ESP İçerik Paneli
local espContent = Instance.new("Frame", contentArea)
espContent.Size = UDim2.new(1, 0, 1, 0)
espContent.BackgroundTransparency = 1
espContent.Visible = false

local espToggleBtn = Instance.new("TextButton", espContent)
espToggleBtn.Size = UDim2.new(0, 200, 0, 50)
espToggleBtn.Position = UDim2.new(0.5, -100, 0, 50)
espToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 60, 200)
espToggleBtn.TextColor3 = Color3.new(1, 1, 1)
espToggleBtn.Font = Enum.Font.GothamBold
espToggleBtn.TextSize = 22
espToggleBtn.Text = "ESP: Kapalı"
Instance.new("UICorner", espToggleBtn).CornerRadius = UDim.new(0, 12)

espToggleBtn.MouseEnter:Connect(function()
    espToggleBtn.BackgroundColor3 = Color3.fromRGB(180, 90, 230)
end)
espToggleBtn.MouseLeave:Connect(function()
    espToggleBtn.BackgroundColor3 = Color3.fromRGB(150, 60, 200)
end)

local espEnabled = false
local espBoxes = {}

local function createBox()
    local box = Instance.new("BoxHandleAdornment")
    box.Adornee = nil
    box.AlwaysOnTop = true
    box.ZIndex = 10
    box.Color3 = Color3.fromRGB(255, 0, 0)
    box.Transparency = 0.5
    box.Size = Vector3.new(4, 6, 1)
    box.Parent = workspace
    return box
end

local function updateESP()
    for playerName, box in pairs(espBoxes) do
        local targetPlayer = Players:FindFirstChild(playerName)
        if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
            box.Adornee = targetPlayer.Character.HumanoidRootPart
            box.Visible = espEnabled
        else
            box.Adornee = nil
            box.Visible = false
        end
    end
end

espToggleBtn.MouseButton1Click:Connect(function()
    espEnabled = not espEnabled
    espToggleBtn.Text = espEnabled and "ESP: Açık" or "ESP: Kapalı"

    if espEnabled then
        for _, plr in pairs(Players:GetPlayers()) do
            if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                if not espBoxes[plr.Name] then
                    espBoxes[plr.Name] = createBox()
                end
            end
        end
    else
        for _, box in pairs(espBoxes) do
            box.Visible = false
        end
    end
end)

Players.PlayerAdded:Connect(function(plr)
    plr.CharacterAdded:Connect(function(char)
        if espEnabled then
            if not espBoxes[plr.Name] then
                espBoxes[plr.Name] = createBox()
            end
        end
    end)
end)

Players.PlayerRemoving:Connect(function(plr)
    if espBoxes[plr.Name] then
        espBoxes[plr.Name]:Destroy()
        espBoxes[plr.Name] = nil
    end
end)

RunService.RenderStepped:Connect(function()
    if espEnabled then
        updateESP()
    end
end)

-- Menü değişimi fonksiyonu
local function setActiveMenu(menu)
    contentTitle.Text = menu
    homeContent.Visible = false
    jumpContent.Visible = false
    speedContent.Visible = false
    noclipContent.Visible = false
    espContent.Visible = false

    if menu == "Home" then
        homeContent.Visible = true
    elseif menu == "Jump" then
        jumpContent.Visible = true
    elseif menu == "Speed" then
        speedContent.Visible = true
    elseif menu == "Noclip" then
        noclipContent.Visible = true
    elseif menu == "ESP" then
        espContent.Visible = true
    end
end

homeBtn.MouseButton1Click:Connect(function()
    setActiveMenu("Home")
end)
jumpBtn.MouseButton1Click:Connect(function()
    setActiveMenu("Jump")
end)
speedBtn.MouseButton1Click:Connect(function()
    setActiveMenu("Speed")
end)
noclipBtn.MouseButton1Click:Connect(function()
    setActiveMenu("Noclip")
end)
espBtn.MouseButton1Click:Connect(function()
    setActiveMenu("ESP")
end)

setActiveMenu("Home")

-- Sürüklenebilirlik kodu
local dragging = false
local dragInput, dragStart, startPos

mainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

mainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)
